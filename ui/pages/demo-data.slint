export struct DemoListRowData {
    title: string,
    detail: string,
    badge: string,
    status: string,
    selected: bool,
}

export struct ThemeOptionData {
    id: string,
    label: string,
    summary: string,
    accent_hex: string,
    selected: bool,
}

export struct PatternNoteData {
    title: string,
    category: string,
    recommendation: string,
    less_ideal: string,
}

export struct ComponentMetaData {
    id: string,
    title: string,
    category: string,
    default_visible: bool,
    recommended: bool,
}
